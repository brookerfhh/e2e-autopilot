# deps
node_modules/

# secrets — NEVER commit (live session cookies)
scripts/test-auth/auth.json
.tmp-storage.json

# build-flow output — per-user data-setup scripts, bootstrapped by the build-flow skill
# (incl. flows/FLOWS.md, the recordings and throwaway runners). Not committed.
flows/

# generated reports / artifacts
playwright-report/
monocart-report/
test-results/
blob-report/
.playwright/

# stray diagnostic scripts the recon step may create
scripts/regression/__diag.ts
scripts/regression/results-*.json
scripts/regression/failure-*.png

# editor / OS
.DS_Store
.idea/
.vscode/
