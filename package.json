{
  "name": "e2e-autopilot",
  "version": "0.1.0",
  "private": true,
  "description": "AI-agent-generated Playwright e2e regression suite for stable admin pages (recon the live page → cases → scripts → verify).",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^20.0.0",
    "monocart-reporter": "^2.11.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
